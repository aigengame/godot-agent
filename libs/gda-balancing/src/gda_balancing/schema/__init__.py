"""The Standard Schema core (bADR-0001..0006).

The pydantic document model under :mod:`gda_balancing.schema.model` is the
single source of truth; the published structural schema and the semantic rule
catalog are generated projections of it (bADR-0005), and the boundary funnel
under :mod:`gda_balancing.schema.funnel` is the one validation boundary every
Design document crosses (bADR-0004).
"""
